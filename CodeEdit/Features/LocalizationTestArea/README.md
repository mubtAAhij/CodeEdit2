# Localization Test Area

This folder is a fixture set for testing Quetzal multi-format localization.

Suggested target directory for pipeline tests:

- `CodeEdit/Features/LocalizationTestArea`

Included file types:

- `.swift` (SwiftUI literals)
- `Info.plist` (usage descriptions and bundle display strings)
- `.strings`
- `.stringsdict`
- `.storyboard`
- `.xib`
- `.md`
- `.txt`
- `.html`
- `.json`
- `.yaml`

Use this area to validate:

1. extraction coverage by source type
2. apply behavior by source type
3. merge/check-run/queue behavior in PR flows
